<!DOCTYPE html>
<html>
  <head>
    <% include ../header.ejs %>
  </head>
  <body>
    <% include ../body.ejs %>
    <br>
    <div class="posts">
    	<div class="show">
        <h3><%= data.title %></h3>
        <br>내용<br>
        <p class="body"><%= data.body %><p>
        <div class="time">게시 날짜: 
          <%= data.createdAt.toLocaleDateString() %>
          <%= data.createdAt.toLocaleTimeString() %>
        </div>
        <% if(data.updatedAt) { %>
          <div class="time">수정 날짜:
            <%= data.updatedAt.toLocaleDateString() %>
            <%= data.updatedAt.toLocaleTimeString() %>
          </div>
        <% } %>
        <div class="tools">
          <a href="/posts/<%=data._id%>/edit"><button>수정</button></a>
          <form class="btn_delete" action="/posts/<%=data._id%>?_method=delete" method="post">
            <button type="submit">삭제</button>
          </form>
        </div>
        <a href="/posts"><button>취소</button></a>
      </div>
    </div>
  </body>
</html>